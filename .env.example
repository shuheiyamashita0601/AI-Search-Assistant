# 環境変数設定例（開発環境用）
# 本番環境では必ずすべての値を変更してください

# ===========================================
# アプリケーション基本設定
# ===========================================
NODE_ENV=development
PORT=3001

# ===========================================
# データベース設定
# ===========================================
# PostgreSQL接続URL
DATABASE_URL="postgresql://postgres:password@localhost:5432/test_ai_search_assistant"

# ===========================================
# Redis設定
# ===========================================
# Redis接続URL
REDIS_URL="redis://:redispassword@localhost:6379"

# ===========================================
# 認証・セキュリティ設定
# ===========================================
# JWT署名用秘密鍵（本番では32文字以上のランダム文字列に変更）
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# JWT有効期限（秒）
JWT_EXPIRES_IN=86400

# ===========================================
# 外部API設定
# ===========================================
# OpenAI API キー
OPENAI_API_KEY="sk-your-openai-api-key-here"

# OpenAI組織ID（オプション）
OPENAI_ORGANIZATION=""

# ===========================================
# フロントエンド設定
# ===========================================
# フロントエンドURL（CORS設定用）
FRONTEND_URL="http://localhost:3000"

# ===========================================
# Next.js環境変数（NEXT_PUBLIC_で始まる変数はクライアントサイドでも利用可能）
# ===========================================
# バックエンドAPI URL
NEXT_PUBLIC_API_URL="http://localhost:3001/api/v1"

# WebSocket URL（将来のリアルタイム機能用）
NEXT_PUBLIC_WS_URL="ws://localhost:3001"

# アプリケーション名
NEXT_PUBLIC_APP_NAME="AI検索アシスタント"

# ===========================================
# ログ設定
# ===========================================
# ログレベル（error, warn, info, debug）
LOG_LEVEL=info

# ログファイル出力先
LOG_FILE_PATH=./logs/app.log

# ===========================================
# レート制限設定
# ===========================================
# API呼び出し制限（分あたりのリクエスト数）
RATE_LIMIT_MAX=100

# レート制限時間枠（分）
RATE_LIMIT_WINDOW=1

# ===========================================
# ファイルアップロード設定
# ===========================================
# 最大ファイルサイズ（MB）
MAX_FILE_SIZE=10

# アップロード先ディレクトリ
UPLOAD_DIR=./uploads

# ===========================================
# メール設定（将来のメール機能用）
# ===========================================
# SMTP設定
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
